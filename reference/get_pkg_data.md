# Get package data

Get package data

## Usage

``` r
get_pkg_data(packages = NULL)
```

## Arguments

- packages:

  Character vector of package names (default is NULL, uses loaded
  packages)

## Value

A list containing logopaths and urls for the packages
